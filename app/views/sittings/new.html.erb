<h1><%= @quiz.name %></h1>
<p><%= @quiz.instructions %></p>

<%= form_for :response, url: quiz_sittings_path(@quiz) do |f| %>
  <% @questions.each_with_index do |question, i| %>

    <%= render 'question', f: f, question: question, i: i %>

  <% end %>

  <%= submit_tag "Submit" %>
<% end %>

