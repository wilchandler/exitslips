require 'spec_helper'

describe SittingsController do

	# before(:each) do 
 #    quiz = Quiz.create!(name: "MyQuiz", instructions: "My Instructions", section: section, requirement_id: 1)		
	# end

	# describe '#new' do

	# 	before(:each) { xhr :post, :create, id: quiz.id, sort_type: 'homogenous', group_size: '3' }

	# 	it 'finds a quiz based '
	# 		get :new

	# end

	# describe '#create' do
 #    before(:each) { xhr :post, :create, id: quiz.id, sort_type: 'homogenous', group_size: '3' }

	# end

end
